pipeline {
        
    agent {label "slave-ssh"}
        
    stages {
       stage("Clone") {
           steps {
           }
       }
       
       stage("Build") {
           steps {
           }
       }
       
       stage("Test") {
           steps {
           }
       }
    }
    
}
