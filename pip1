pipeline {
        
    agent {label "slave-ssh"}
        
    stages {
       stage("Clone") {
           steps {
            git clone https://github.com/L0rhammer/jenkinscripts.git
           }
       }
       
       stage("Build") {
           steps {
            sh ./build.sh
           }
       }
       
       stage("Test") {
           steps {
           }
       }
    }
    
}
